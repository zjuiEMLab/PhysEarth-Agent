# Secrets
.env
.env.*
!.env.example
*.token

# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.venv/
venv/
.uv/

# Tooling
.pytest_cache/
.ruff_cache/
.mypy_cache/
.ipynb_checkpoints/

# Local data and artifacts
data/cache/
data/downloads/
runs/
outputs/
output_no_cascades/
*.log
*.err
*.out

# The evaluation records are the evidence behind REPORT.md and are committed on purpose.
!evaluation/results/runs/

# OS and editors
.DS_Store
Thumbs.db
.vscode/
.idea/

# Local runtime state
_state/

# Isolated environment for the numpy-2 model, never part of the deployment
.venv-pws/

# Local holding area: never tracked, never deployed
dummy/

# pywatershed writes these into the working directory
*_mass_budget.nc

# Claude Code: the dev-server config is shared, local permissions are not
.claude/settings.local.json
