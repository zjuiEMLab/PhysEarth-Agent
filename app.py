"""Entry point. The ModelScope deployspec pins this filename, so it stays at the root.

Everything it does lives in frontend/studio.py.
"""

from frontend.studio import demo, main

if __name__ == "__main__":
    main()
