"""Everything the visitor sees: the Gradio wiring, the views, the stylesheet.

Nothing in here may import the package directly. It goes through `physearth.api`, which
is the declared surface between the interface and the agent.
"""
