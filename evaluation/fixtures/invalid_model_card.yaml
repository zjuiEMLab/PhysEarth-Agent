name: broken_demo
version: "0.0"
description: Intentionally invalid card used only by the registration evaluation.
tier: demo
entrypoint: adapter:run
parameters:
  density:
    type: number
    description: Deliberately missing a unit and carrying reversed bounds.
    minimum: 900.0
    maximum: 1.0
outputs:
  value:
    description: Deliberately missing a unit.

