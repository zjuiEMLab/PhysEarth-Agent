# T1 — Model onboarding

Measures whether a contributor can register a previously unseen model correctly: manifest,
adapter, model card, validation rules, minimal run, and evidence contract. The public fixture
is `models/examples/toy_model`; a competition-quality benchmark should add a hidden fixture and
mutation tests so hard-coded registrations cannot pass.

Competition status: **optional/design-only in the two-day window**.
